# Gemini Ethos Configuration
# Copy this file to .env and configure your values

# Required: Google Cloud Project ID
GOOGLE_CLOUD_PROJECT=turismo-486820

# Optional: Google Cloud Region (defaults to us-central1)
GOOGLE_CLOUD_LOCATION=us-central1

# Optional: Server port (defaults to 8080)
ETHOS_SERVER_PORT=8080

# Optional: Path to service account credentials JSON
# If not set, uses Application Default Credentials (ADC)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json
